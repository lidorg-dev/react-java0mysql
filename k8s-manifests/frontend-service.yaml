apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: default
  labels:
    argocd.argoproj.io/instance: react-java0mysql
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 3000
  selector:
    app: frontend
